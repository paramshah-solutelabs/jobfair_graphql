- name: jobfair
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://postgres:postgres@postgres:5432/jobfair
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include jobfair/tables/tables.yaml"
